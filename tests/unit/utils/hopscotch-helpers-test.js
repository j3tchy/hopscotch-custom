import hopscotchHelpers from 'marvel-app/utils/hopscotch-helpers';
import { module, test } from 'qunit';

module('Unit | Utility | hopscotch helpers');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hopscotchHelpers();
  assert.ok(result);
});
